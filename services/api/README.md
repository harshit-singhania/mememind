# MemeMind API Service

This is the backend service for MemeMind, built with FastAPI.

## Running Locally

```bash
pip install -r requirements.txt
uvicorn app.main:app --reload
```

## Testing

```bash
# Run tests using module execution to handle imports correctly
python -m pytest
```
